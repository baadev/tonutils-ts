// prepare config object
export const config = {
    LiteserverPath: "global.config.json",
    LiteserverCofigURL: "", // if defined, will be used to fetch config from specified URL, otherwise will use LiteserverPath
};
